# CloudClass 新功能使用说明

说明这次项目新增了什么、怎么配置、怎么启动，以及答辩或演示时可以怎么展示这些功能。

## 这次新增了哪些功能

这次更新主要是围绕“教师端功能增强”来做的，目标是让系统看起来更像一个完整的网络教学平台，而不是只有课程、作业、讨论这几个基础模块。

### 1. 教学资料改进

以前课程资料里的“外部链接”会直接跳到第三方网站，这样有两个问题：

- 演示时内容不可控，点开可能是别的网站
- 看起来不像自己系统的一部分

现在改成了“站内资料详情页”，也就是说：

- 教师可以上传资料标题、说明、正文内容
- 资料可以有文件，也可以有参考链接
- 学生和教师点击后先进入平台自己的资料详情页

这样看起来更完整，也更适合做课程项目展示。

### 2. 教师批改页增强

以前老师批改作业时，只能看到评分表单，不方便知道学生到底提交了什么。

现在批改页新增了这些信息：

- 学生姓名
- 提交时间
- 提交说明
- 提交附件下载链接
- AI 辅助批阅入口

另外，作业提交列表里也增加了：

- 是否有附件
- 是否已批改
- 成绩导出 CSV

### 3. AI 教师助手

系统新增了一个“AI 助手中心”，教师可以直接使用统一入口完成不同类型的 AI 教学辅助。

支持的模式有：

- AI 备课：生成教学目标、知识点大纲、课堂活动建议
- AI 授课：生成授课提纲、互动问题、随堂练习
- AI 评价：生成学生评价草稿和改进建议
- AI 辅助批阅：给出建议分数和评语草稿
- AI 查重：对同一份作业的不同学生提交内容做文本相似度分析

注意：

- AI 的结果默认只是“建议”，不会直接替老师打分
- 这样更安全，也更符合“AI 辅助教学”的定位

### 4. 签到功能

新增了课堂签到模块，老师可以发起签到，学生可以进入课程后完成签到。

支持的基本流程是：

- 教师创建一次签到
- 学生在课程页面点击签到
- 教师查看已签到和未签到名单

这个功能很适合演示“课堂管理能力”。

### 5. 分组功能

新增了课程分组功能，老师可以手动创建小组并把学生分进去。

作用包括：

- 展示课程组织能力
- 为后续扩展分组讨论、分组作业打基础

### 6. 讨论功能增强

原本系统就有讨论区，这次增加了“讨论类型”：

- 普通讨论
- 课堂讨论
- 分组讨论

这样会让教学场景看起来更完整，不再只是一个简单论坛。

### 7. 成绩导出

老师可以把某次作业的成绩导出为 CSV 文件。

导出的内容包括：

- 学生姓名
- 学号/工号
- 用户名
- 提交时间
- 附件状态
- 分数
- 教师评语
- 批改时间

这个功能很适合在答辩时展示“教学数据管理能力”。

## 这次改动涉及到哪些数据库变化

如果你是第一次接触 Django，可以把“数据库变化”简单理解成：

“系统多加了几个数据表，用来存这些新功能需要的数据。”

### 新增的数据表

这次主要新增了下面这些模型：

- `AIInteractionLog`
  - 用来保存 AI 助手的使用记录
  - 记录教师用了哪个 AI 模式、输入了什么、AI 返回了什么

- `AttendanceSession`
  - 用来表示一次签到活动

- `AttendanceRecord`
  - 用来表示某个学生是否参加了某次签到

- `CourseGroup`
  - 用来表示课程里的一个小组

- `CourseGroupMember`
  - 用来表示小组成员关系

### 修改的数据表

- `CourseMaterial`
  - 新增了 `content` 字段，用来存放站内资料正文
  - `external_url` 的含义从“外部链接”调整为“参考链接”
  - `material_type` 里的链接类型改成了“站内链接资料”

- `DiscussionPost`
  - 新增了 `post_type` 字段，用来区分普通讨论、课堂讨论、分组讨论

### 为什么要执行迁移

因为 Django 的模型变了，数据库也要跟着变。

所以每次你拉到这版代码后，都需要执行：

```bash
./.venv/bin/python manage.py migrate
```

这一步的作用就是把新增的表和字段真正创建到数据库里。

## 新增了哪些配置

这次新增的重点配置主要是 AI 千问相关配置。

### 1. `.env` 里建议新增的配置

你可以在项目根目录的 `.env` 文件中加入下面这些内容：

```env
DASHSCOPE_API_KEY=你的千问API密钥
DASHSCOPE_MODEL=qwen-plus
```

说明：

- `DASHSCOPE_API_KEY`
  - 这是调用千问接口最重要的配置
  - 如果没有这个值，AI 页面仍然能打开，但是会返回本地演示文案，而不是真正调用千问

- `DASHSCOPE_MODEL`
  - 这是可选配置
  - 默认已经写成 `qwen-plus`
  - 如果你后面想切换别的千问模型，可以改这里

### 2. 原有数据库配置

项目原本就支持 SQLite 和 MySQL。

如果你只是本地快速演示，推荐继续用 SQLite，最简单。

如果你想按 README 里的完整方式运行 MySQL，可以在 `.env` 中配置：

```env
DB_ENGINE=mysql
DB_NAME=cloudclass
DB_USER=root
DB_PASSWORD=你的密码
DB_HOST=127.0.0.1
DB_PORT=3306
```

如果不配置这些内容，默认就是 SQLite。

## 应该如何配置和启动项目

下面给一个最适合本地演示的启动流程。

### 第一步：创建虚拟环境

```bash
python3 -m venv .venv
```

### 第二步：激活虚拟环境

macOS / Linux：

```bash
source .venv/bin/activate
```

Windows：

```bash
.venv\Scripts\activate
```

### 第三步：安装依赖

```bash
pip install -r requirements.txt
```

### 第四步：准备环境变量

先复制一份环境变量模板：

```bash
cp .env.example .env
```

然后按需要修改 `.env`。

如果只是本地跑项目，最低限度只要：

- 使用默认 SQLite
- 可选增加千问配置

示例：

```env
DEBUG=True
DASHSCOPE_API_KEY=你的千问API密钥
DASHSCOPE_MODEL=qwen-plus
```

### 第五步：执行数据库迁移

```bash
./.venv/bin/python manage.py migrate
```

### 第六步：初始化演示数据

```bash
./.venv/bin/python manage.py seed_demo
```

这一步会自动创建：

- 管理员账号
- 教师账号
- 学生账号
- 样例课程
- 样例资料
- 样例作业
- 样例讨论
- 样例签到
- 样例分组

也就是说，不需要你手动一条一条录入数据，适合快速演示。

### 第七步：启动项目

```bash
./.venv/bin/python manage.py runserver
```

然后浏览器打开：

```text
http://127.0.0.1:8000/
```

## 演示账号

项目内置了演示账号：

- 管理员：`admin / admin123456`
- 教师：`teacher01 / teacher123456`
- 学生：`student01 / student123456`

建议答辩时重点使用：

- 教师账号：展示教师端新功能
- 学生账号：配合展示签到、课程查看、作业提交

## 演示时建议怎么展示

如果是给老师答辩或者课程验收，建议不要乱点，最好按一条“故事线”来演示。

下面给你一个比较顺的演示流程。

### 演示路线一：先展示教师工作台升级

登录教师账号后，先进入“教师工作台”，重点介绍：

- 新增了 AI 助手入口
- 新增了签到管理入口
- 新增了分组管理入口
- 可以导出成绩
- 待批改作业更清楚

这个时候可以先说一句：

“这次我重点增强的是教师端，把原来比较基础的教学平台，扩展成了更完整的课堂教学系统。”

### 演示路线二：展示课程资料不再是第三方链接

进入某门课程，打开“教学资料”。

重点展示：

- 点击资料后，进入的是系统自己的资料详情页
- 页面里可以看到资料说明、正文、文件、参考链接

这里可以说明：

“我把原先直接跳第三方网站的外链，改成了站内资料详情页，这样平台更完整，也更适合演示和教学使用。”

### 演示路线三：展示 AI 助手

进入“AI 助手中心”，建议演示两个最容易看懂的功能：

- AI 备课
- AI 辅助批阅

#### 演示 AI 备课

选择一门课程，输入类似内容：

```text
请为《Python Web 开发基础》生成一份 45 分钟课堂教案，包含教学目标、重点难点、课堂互动和课后作业建议。
```

然后展示 AI 输出结果。

#### 演示 AI 辅助批阅

进入某个作业的批改页面，点击“生成建议分数与评语”。

重点讲：

- AI 会根据作业说明和学生提交内容给出建议
- 最终分数仍然由教师手动保存

这样老师会觉得你考虑了“AI 能辅助，但不能乱自动打分”这个问题。

### 演示路线四：展示签到功能

用教师账号进入课程，发起一条签到。

然后切换到学生账号：

- 进入同一门课程
- 点击“立即签到”

再切回教师账号：

- 打开签到详情
- 展示已签到和未签到名单

这个流程很直观，演示效果也很好。

### 演示路线五：展示分组功能

进入分组管理，展示：

- 可以创建小组
- 可以把学生加到小组里
- 课程详情页能看到小组成员

这里可以讲：

“这样后面如果要继续扩展分组作业、分组讨论，会比较方便。”

### 演示路线六：展示成绩导出

进入某个作业的提交列表，点击“导出成绩 CSV”。

演示时可以说明：

- 老师可以直接把成绩整理出来
- 便于课程管理和成绩统计

## 如果 AI 没有配置好怎么办

这个项目做了一个比较友好的处理：

- 如果你配置了 `DASHSCOPE_API_KEY`，系统会尝试调用千问
- 如果没配置，AI 页面也不会报错
- 系统会返回本地演示文案，保证答辩时页面仍然能展示功能

这对课程项目很有帮助，因为有时候答辩环境不一定能联网或者 API 不一定可用。

## 给同学的简单理解版本

你可以把这次升级理解成一句话：

“原本这是一个基础的课程平台，现在我把它升级成了一个更像真实教学系统的教师教学平台，补上了 AI、签到、分组、资料详情、成绩导出这些更完整的教学功能。”

## 最后建议

如果你要拿这个项目去答辩，建议你提前准备好下面这些内容：

- `.env` 已经配好
- 数据库已经迁移完成
- 演示数据已经导入
- 至少准备一个教师账号和一个学生账号
- AI 助手最好提前试一次
- 签到和分组的演示路径提前走一遍

这样答辩时会更顺，不容易临场出问题。
